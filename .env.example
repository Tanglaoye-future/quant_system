# 复制为 .env 后填写（.env 已被 gitignore）
# Postgres = 运营真相源（runs/signals/positions/journal）

POSTGRES_USER=quant
POSTGRES_PASSWORD=quant
POSTGRES_DB=quant
POSTGRES_PORT=5432

# backend / compute / alembic 统一从这里读连接串
DATABASE_URL=postgresql+psycopg://quant:quant@localhost:5432/quant
